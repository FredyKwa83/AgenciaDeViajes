<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://kit.fontawesome.com/330ca645c0.js" crossorigin="anonymous"></script>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:ital,wght@0,400;0,500;1,300;1,400&display=swap"
        rel="stylesheet">
    
    <link rel="stylesheet" href="/css/stylus.css">
    
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-9ndCyUaIbzAi2FUVXJi0CjmCapSmO7SnpJef0486qhLnuZ2cdeRhO02iuK6FUUVM" crossorigin="anonymous">
    
    
    <title> Editar producto </title>
</head>
<body class="fondo">
    <header>
        <%- include('./partials/header') %>
    </header>
    
    <main class="main-cart">
        <form action="/edit/<%=libros.id%>?_method=PUT" method="POST">
            <legend>Formulario de Edicion de libros</legend>
            <div class="mb-3">
                <label for="name" class="form-label">Nombre del producto:</label>
				<input type="text" id="name" name="titulo" class="form-control" value="<%= libros.nombre %>" placeholder="Nombre del producto" >
                <p class="erroresEdit" id="error1"></p>
            </div>
            <div class="mb-3">
                <label for="price" class="form-label">Precio del producto:</label>
				<input type="text" id="price" name="precio" class="form-control" value="<%= libros.precio %>" placeholder="Precio del producto">
                <p class="erroresEdit" id="error2"></p>
            </div>
            <div class="mb-3">
                <label for="discount" class="form-label">Descuento:</label>
				<input type="text" id="discount" name="descuento" class="form-control" value="<%= libros.descuento %>"placeholder="Descuento">
                <p class="erroresEdit" id="error3"></p>
            </div>
            <div class="mb-3">
                <label for="category" class="form-label">Genero:</label>
						<select name="genero" id="category" class="select-editForm" class="form-control">

                            <% for (let i=0; i < generos.length; i++) {%>
                                <% if (generos[i].id == libros.id_categoria) { %>

                                    <option value="<%= generos[i].id %>" selected> <%= generos[i].nombre %></option>

                                <% } else { %>
                                    <option value="<%= generos[i].id %>"> <%= generos[i].nombre %></option>
                                <% } %>
                            <% } %>    
						</select>
                        <p class="erroresEdit" id="error4"></p>
            </div>
            <div class="mb-3">
                <label for="description" class="form-label">Descipción:</label>
				<textarea name="descripcion" id="description" class="form-control"> <%= libros.descripcion %>"</textarea>
                <p class="erroresEdit" id="error5"></p>
            </div>
            <div class="mb-3">
                <label for="name" class="form-label">Imagen:</label>
                
				
            </div>
            <div class="mb-3">
                <a href="/edit/<%=libros.id%>">
                <button type="submit" class="btn btn-primary">Editar Libro</button></a>
            </div>
            
            
          </form>
           
    </main>
    
        <%- include('./partials/footer') %>
  
   <script src="/scripts/validacionesEdit.js" defer></script>
</body>
</html>